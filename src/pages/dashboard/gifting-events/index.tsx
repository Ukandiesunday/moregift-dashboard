import styles from "../innerpages.module.scss";
import Layout from "../layout";

const GiftingEventPage = () => {
  return (
    <Layout>
      <div>GreetingEventPage</div>
    </Layout>
  );
};

export default GiftingEventPage;
